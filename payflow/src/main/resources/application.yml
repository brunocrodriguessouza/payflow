spring.application.name=PayFlow

spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/payflowdb
        username: admin
        password: admin
        driver-class-name: org.postgresql.Driver

jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
        ddl-auto: update
    show-sql: false
